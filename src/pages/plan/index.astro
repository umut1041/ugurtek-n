---
import MainLayout from "../../layouts/MainLayout.astro";
import planData from "../../data/plan/structure.json";

type Week = (typeof planData.weeks)[number];
const weeks: Week[] = planData.weeks;
---

<MainLayout
  title="Çalışma Planı"
  description="Goethe A2 sınavına hazırlık için haftalık yapılandırılmış çalışma planı."
>
  <section>
    <h1>Çalışma Planı</h1>
    <p>
      Aşağıdaki plan, A1 düzeyindeki temelini A2 Goethe sınavını geçecek seviyeye
      taşımak için haftalık hedefler sunar. Her haftada gramer, kelime ve sınav
      odaklı görevler bulunur.
    </p>
    <h2>Bu sayfada ne öğreneceksin?</h2>
    <ul>
      <li>8–12 haftalık yapılandırılmış program: her hafta net hedefler</li>
      <li>Haftalık görevler: Gramer, Kelime, Lesen, Schreiben, Sprechen</li>
      <li>Görevleri işaretleyerek ilerlemeni takip etme (cihazında saklanır)</li>
    </ul>
    <p>
      Bir haftaya tıklayarak o haftanın hedeflerini ve görev listesini gör; her görevi
      tamamladıkça işaretle ve “Bu haftayı tamamladım” butonuyla haftayı kapat.
    </p>
  </section>

  <section class="cards-grid">
    {weeks.map((week) => (
      <a href={`/plan/${week.id}`} class="card">
        <h3>{week.title}</h3>
        <p>{week.description}</p>
        <span class="card-link">Hafta sayfasına git →</span>
      </a>
    ))}
  </section>
</MainLayout>

